{
  "id": "lh08g4qfhyac0538uesqanoz",
  "slug": "podklyucheniyu-flask-prilozheniya-k-my-sql",
  "title": "Подключению Flask-приложения к MySQL",
  "description": "",
  "body": "## 📦 1. Установка зависимостей\n\n```bash\npip install flask-sqlalchemy flask-migrate pymysql\n```\n\n> `pymysql` — драйвер для работы с MySQL в Python.\n\n---\n\n## 🗃️ 2. Настройка `.env` файла\n\nСоздай файл `.env` в корне проекта:\n\n```env\nFLASK_APP=run.py\nFLASK_ENV=development\nDATABASE_URL=mysql+pymysql://username:password@localhost/dbname\n```\n\nЗамени:\n- `username` — имя пользователя MySQL\n- `password` — пароль\n- `dbname` — имя базы данных\n\n---\n\n## 🔧 3. Конфигурация приложения\n\nВ файле `app/__init__.py`:\n\n```python\nfrom flask import Flask\nfrom flask_sqlalchemy import SQLAlchemy\nfrom flask_migrate import Migrate\nfrom flask_cors import CORS\nfrom dotenv import load_dotenv\nimport os\n\n# Инициализация расширений\ndb = SQLAlchemy()\nmigrate = Migrate()\nload_dotenv()  # Загрузить переменные окружения\n\ndef create_app():\n    app = Flask(__name__)\n    CORS(app)\n\n    # Загрузка конфигурации\n    app.config['SQLALCHEMY_DATABASE_URI'] = os.getenv('DATABASE_URL')\n    app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False\n\n    db.init_app(app)\n    migrate.init_app(app, db)\n\n    # Регистрация маршрутов\n    from .routes.api import api_bp\n    app.register_blueprint(api_bp, url_prefix='/api')\n\n    # Создание таблиц (опционально)\n    with app.app_context():\n        db.create_all()\n\n    return app\n```\n\n---\n\n## 📄 4. Пример модели\n\nСоздай модель в `app/models/user.py`:\n\n```python\nfrom app import db\n\nclass User(db.Model):\n    __tablename__ = 'users'\n\n    id = db.Column(db.Integer, primary_key=True)\n    username = db.Column(db.String(80), unique=True, nullable=False)\n    email = db.Column(db.String(120), unique=True, nullable=False)\n\n    def __repr__(self):\n        return f'<User {self.username}>'\n```\n\n> Не забудь импортировать модель в `app/__init__.py`, чтобы миграции её увидели:\n```python\nfrom app.models.user import User\n```\n\n---\n\n## 🔄 5. Миграции\n\nВыполни команды в терминале:\n\n```bash\nflask db init\nflask db migrate -m \"Initial migration\"\nflask db upgrade\n```\n\nТеперь таблица `users` будет создана в вашей MySQL БД.\n\n---\n\n## 🌐 6. Пример маршрута с использованием БД\n\nФайл `app/routes/api.py`:\n\n```python\nfrom flask import Blueprint, jsonify, request\nfrom app.models.user import User\nfrom app import db\n\napi_bp = Blueprint('api', __name__)\n\n@api_bp.route('/users', methods=['GET'])\ndef get_users():\n    users = User.query.all()\n    return jsonify([{'id': u.id, 'username': u.username, 'email': u.email} for u in users])\n\n@api_bp.route('/users', methods=['POST'])\ndef add_user():\n    data = request.get_json()\n    new_user = User(username=data['username'], email=data['email'])\n    db.session.add(new_user)\n    db.session.commit()\n    return jsonify({'message': 'User added'}), 201\n```\n\n---\n\n## ▶️ 7. Запуск сервера\n\nВ файле `run.py`:\n\n```python\nfrom app import create_app\n\napp = create_app()\n\nif __name__ == '__main__':\n    app.run(debug=True)\n```\n\nЗапуск:\n\n```bash\npython run.py\n```\n\nAPI будет доступен по адресу:  \n👉 `http://localhost:5000/api/users`\n\n---\n\n## ✅ Полезные советы\n\n- Убедись, что MySQL-сервер запущен.\n- Пользователь MySQL должен иметь права на указанную БД.\n- Для продакшена используй пул соединений (например, `SQLAlchemy + connection pool`).\n- Добавь обработку ошибок и валидацию данных.",
  "cover": "/media/cover.webp",
  "category": {
    "slug": "dlya-vzroslyh",
    "name": "Для взрослых"
  },
  "subcategory": {
    "slug": "veb-razrabotka",
    "name": "Веб-разработка"
  },
  "url": "/articles/dlya-vzroslyh/veb-razrabotka/podklyucheniyu-flask-prilozheniya-k-my-sql",
  "publishedAt": "2026-04-26T23:31:57.203Z",
  "views": 1
}